# Логотипы интеграций

`bitrix24.svg`, `amocrm.svg` и `sheets.svg` — буквенные знаки в фирменных цветах,
а не официальные логотипы: подделывать чужой знак нельзя. Чтобы поставить
настоящий, положите файл с тем же именем — разметка его подхватит без правок.
